{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "deploy": {
    "startCommand": "php artisan storage:link --force && php artisan migrate --force && php artisan db:seed --class=SuperAdminSeeder --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && PHP_CLI_SERVER_WORKERS=${PHP_CLI_SERVER_WORKERS:-2} PHP_INI_SCAN_DIR=\"${PHP_INI_SCAN_DIR:-}:/app/deploy/php\" php artisan serve --no-reload --host=0.0.0.0 --port=${PORT:-8000}"
  }
}
