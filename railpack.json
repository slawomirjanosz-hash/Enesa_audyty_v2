{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "deploy": {
    "startCommand": "php artisan storage:link --force && php artisan migrate --force && php artisan db:seed --class=SuperAdminSeeder --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
  }
}
